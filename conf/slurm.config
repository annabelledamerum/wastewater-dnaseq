params {
    partition = null
    database_location = '/mnt/genomes/zymobiomics/WGS_Pipeline/shotgun_database'
    max_memory = '240.GB'
    max_cpus = 64
}

process {
    executor = 'slurm'
    queue = params.partition
}

apptainer.enabled = true
docker.enabled = false